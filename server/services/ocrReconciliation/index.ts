/**
 * OCR Reconciliation Module
 * 
 * Provides joint OCR reconciliation with targeted region re-processing
 * and confidence calibration for the audit pipeline.
 */

export * from './types';
export * from './reconciliationService';
