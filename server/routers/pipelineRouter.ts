/**
 * Compatibility re-export — Stage 5 pipeline router is QUARANTINED.
 * Source of truth: `./_quarantine/pipelineRouter` (not mounted on appRouter).
 */
export * from "./_quarantine/pipelineRouter";
