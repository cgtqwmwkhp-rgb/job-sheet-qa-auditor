import EntraSignIn from "@/pages/EntraSignIn";

/** Staff sign-in — portal-verbatim Entra screen (Phase 0). */
export default function Login() {
  return <EntraSignIn subtitle="Job Sheet QA Auditor" redirectPath="/" />;
}
